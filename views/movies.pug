extends layout

block content
  h2= title
  - var movies=results
  div(class="container-fluid")
    div(class="row")
        each m,index  in movies
            div(class="col-sm-2" onclick =`buy(${index})`) 
                p MovieName: #{m.movie_name}, 
                p MovieDirector: #{m.movie_director}, 
                p MovieTiming: #{m.movie_timing}
  script.
    function buy(mi)
    {
        alert(`You watched a film ${mi+1}`)
    }