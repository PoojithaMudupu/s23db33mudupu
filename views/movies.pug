extends layout

block content
  h1= title
  <br>
   - let Create = `dispatch(\"/movies/create\")`
   button.btn( class="btn-primary ml-1", onclick= Create) Create
  - let items = results;
  div(class="container-fluid")
    div(class="row")
      each item,index in items
        - let destination = `dispatch(\"/movies/update?id=${item._id}\")`
        - let detail = `dispatch(\"/movies/detail?id=${item._id}\")`
        - let Delete = `dispatch(\"/movies/delete?id=${item._id}\")`
        div(class="col-sm-2" class="m-1")
          h3 #{item.movie_name}
          p #{item.movie_director}
          p #{item.movie_timing}
          button.btn( class="btn-primary ml-1", onclick= destination) Update
          br
          br
          button.btn( class="btn-success ml-1", onclick= detail) Details
          br
          br
          button.btn( class="btn-secondary ml-1", onclick= Delete) Delete
  script.
    function dispatch(path){
      console.log(path);
      window.location.href=path;
    }